# 站点信息架构（首页导流与页面结构）

## 首页模块与跳转
- **首屏 Hero**：核心卖点 + 双 CTA  
  - 按钮1：`立即学习/免费体验` → `/course`（课程总览页或免费入门章节）  
  - 按钮2：`获取模板/咨询` → `/templates`（模板页）或 `/contact`（联系/表单）
- **产品组合**（课程 + 模板）  
  - 卡片1：`699/年 · 持续更新课程` → `/course`（含价格、更新节奏、试听/退款政策）  
  - 卡片2：`299/次 · 模板合集` → `/templates`（可标注“课程内包含”）  
  - 说明：课程包含模板，单独购买模板为一次性
- **能力/场景/解决方案**（能力点 + 典型场景）  
  - 能力卡片 → `/course` 中对应章节锚点，或 `/blog` 对应深度文章  
  - 场景卡片 → `/cases`（案例页）或 `/blog` 详细拆解
- **案例/成果**（前后对比、数据徽章）  
  - 每张卡片 → `/cases/<slug>`（详细案例或长文）
- **课程大纲 / 学习路径**  
  - 路径/章节列表 → `/course`（课程总览页，含目录、价格、FAQ、试听）  
  - 试听/试看 → `/course/free` 或某免费章节直达
- **模板与资源**  
  - 免费模板/资料包 → `/templates/free` 或 `/resources`  
  - 付费模板合集 → `/templates`（强调课程内包含）
- **免费内容 / 博客**  
  - 博客列表 → `/blog`；入门文章 → `/blog/n8n-intro` 等  
  - 每篇文章底部导流到 `/course` 与 `/templates`
- **社群 / 订阅 / 留资**  
  - 表单/社群入口 → `/community` 或 `/contact`  
  - 订阅引导：引流品（速查表/小册）换邮箱
- **FAQ / 保障**  
  - 常见问题、退款/试听政策 → `/faq` 或 `/course#faq`
- **页脚**  
  - 导航：`/course` `/templates` `/blog` `/cases` `/community` `/contact` `/faq` `/privacy` `/terms`

## 主要页面说明
- `/`（首页）：以上模块的整合与导流
- `/course`：课程落地页，含价格（699/年）、更新节奏、大纲、试看、FAQ、购买/咨询双 CTA
- `/templates`：模板合集页，价格（299 单次），标注“课程内已包含”；可分免费/付费区
- `/blog`：SEO 文章列表；文章内链到课程/模板/案例
- `/cases`：案例列表；详细页 `/cases/<slug>`
- `/community`：社群与订阅入口；表单/二维码/预约
- `/contact`：咨询表单/邮箱/社媒
- `/faq`：FAQ；可在 `/course` 内复用锚点
- `/resources`（可选）：免费资料包、速查表、工具清单
- `/privacy` `/terms`：合规页面

## 路由与 CTA 对应关系
- 首屏 CTA：`/course` + `/templates`（或 `/contact`）  
- 课程卡片/路径区：`/course`（默认）、免费试听指向 `/course/free`  
- 模板卡片：`/templates`（标注课程包含）  
- 案例卡片：`/cases/<slug>`  
- 免费资源/引流：`/resources` 或 `/templates/free`  
- 文章页导流：文末固定 CTA → `/course` 与 `/templates`

---

## 当前站点已有模块（主页）
- 导航栏：Logo + 个人介绍/项目作品/常用工具/N8N 课程（部分锚点尚未对应实区块）。  
- 首屏 Hero：标题、副标题、双 CTA（课程/资料包）、标签组、头像卡。  
- 能力区：AI 工作流、智能体、多平台数据、诊断咨询。  
- 场景区：电商换装、内容发布、公众号排版、私域、Dashboard、矩阵运营等。  
- 产品区：n8n 系统课、模板库、定制项目、咨询/顾问。  
- 案例区：3 个案例卡（电商、内容、私域）。  
- 内容区：博客/知识库、小红书、视频教程、免费文档。  
- 社群/联系方式：社群 CTA + 联系入口，页脚包含社媒和回到顶部。
- 课程目录页：`/course` 列出章节/目录。

## 下一步优先事项（无代码，仅工作清单）
1) **导航与锚点对齐**：首页实际存在的区块（能力、场景、产品、案例、内容、社群）对应导航锚点，去掉无区块的链接或补充对应板块。  
2) **产品与付费入口清晰化**：在首页增加“课程 699/年”和“模板 299/次（课程内含）”双卡或区域，CTA 直达 `/course` 与 `/templates`。  
3) **课程落地页完善**：在 `/course` 加价格、更新节奏、试听/试看、FAQ、购买/咨询双 CTA，并标注模板已包含。  
4) **模板页建立**：新增 `/templates`（含免费/付费分区），并在模板卡注明“课程内已含”。  
5) **案例与博客导流**：在案例/内容卡和文章末尾加导流到课程与模板，形成 SEO → 付费路径。  
6) **暗色可见性检查**：统一提升暗色模式文字/图标/边框对比度，确保各卡片标题/正文清晰。  
7) **免费资源与留资**：在首页/文章页植入免费资料包/模板领取表单，落地到 `/resources` 或 `/templates/free`，收集邮箱/社群。  
8) **FAQ/保障**：独立 `/faq` 或在 `/course` 追加退款/试听/开课节奏说明。  
9) **法律页**：补充 `/privacy` `/terms` 链接到页脚。***
